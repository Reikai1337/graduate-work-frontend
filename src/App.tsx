import { FC } from "react";

import { Button } from "@mui/material";

export type AppProps = {};

export const App: FC<AppProps> = ({}) => {
  return (
    <div>
      <Button>Hello</Button>
    </div>
  );
};
